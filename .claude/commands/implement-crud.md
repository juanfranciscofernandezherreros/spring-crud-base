---
description: Implement or extend a CRUD capability in this Spring Boot microservice in one pass
---

Use the spring-crud-builder agent to implement the requested feature directly in this repository.

Requirements:
- Follow the existing Spring Boot 3 / Java 17 conventions already present in this codebase.
- Preserve the current Result-based architecture as the reference.
- Implement the full CRUD stack in one pass: model, DTOs, mapper, repository, service, controller, validations, exceptions, tests, OpenAPI, and any relevant observability or Docker changes.
- Verify the result with real Maven commands before finishing.
- Report the outcome with evidence, not just a plan.
